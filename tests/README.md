# Tests

Integration, resilience and protocol tests belong here.

Initial automated validation lives beside the scaffolded packages. Contract schema validation is implemented in `packages/contract-validation/tests` and runs through:

```sh
npm test
npm run validate:contracts
```
