# Endpoint

Node Agent, Adapter SDK and product adapters belong here.

## Projects

- `agent-service` contains the Phase 1 Node Agent TypeScript project shell.

Adapter SDK, TouchDesigner adapter, commissioning UI and installer projects will be added through later scoped tasks.
