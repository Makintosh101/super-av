{
  "name": "super-av",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.4.2",
  "scripts": {
    "check:docs": "node scripts/check-docs.mjs",
    "test": "npm run check:docs && npm run check:contracts && npm run check:db && npm run test:unit",
    "check": "npm run check:docs && npm run check:contracts && npm run check:db && npm run test:unit",
    "check:contracts": "node scripts/check-contracts.mjs",
    "check:db": "node cloud/database/scripts/validate-migrations.mjs",
    "test:unit": "node --test tests/*.test.mjs"
  }
}
